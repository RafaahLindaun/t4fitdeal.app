const fs = require('node:fs');
const read = (file) => fs.readFileSync(file, 'utf8');
const write = (file, value) => fs.writeFileSync(file, value);
function replaceOnce(file, from, to, label) {
  const source = read(file);
  const count = source.split(from).length - 1;
  if (count !== 1) throw new Error(`${label}: expected 1 match in ${file}, got ${count}`);
  write(file, source.replace(from, to));
}

// 1) Staff > Alunos: stable roster. No 15s/focus/visibility refetch, and existing rows stay mounted on explicit refresh.
replaceOnce('scr/pages/AdminArea.tsx', '  useMemo,\n  useState,', '  useMemo,\n  useRef,\n  useState,', 'AdminArea useRef');
replaceOnce('scr/pages/AdminArea.tsx', '  const [studentsLoading, setStudentsLoading] = useState(true);\n  const [studentError, setStudentError] = useState("");', '  const [studentsLoading, setStudentsLoading] = useState(true);\n  const studentsHydratedRef = useRef(false);\n  const [studentError, setStudentError] = useState("");', 'AdminArea hydrated ref');
replaceOnce('scr/pages/AdminArea.tsx', '      setStudentsLoading(true);\n      setStudentError("");', '      if (!studentsHydratedRef.current) setStudentsLoading(true);\n      setStudentError("");', 'AdminArea stable loading');
replaceOnce('scr/pages/AdminArea.tsx', '      } finally {\n        setStudentsLoading(false);\n      }\n    }, 260);', '      } finally {\n        studentsHydratedRef.current = true;\n        setStudentsLoading(false);\n      }\n    }, 260);', 'AdminArea hydration finish');
replaceOnce('scr/pages/AdminArea.tsx', `  useEffect(() => {\n    if (!canManageStudents) return;\n\n    const refresh = () => setReloadKey((current) => current + 1);\n    const interval = window.setInterval(refresh, 15000);\n\n    const handleVisibility = () => {\n      if (document.visibilityState === "visible") refresh();\n    };\n\n    window.addEventListener("focus", refresh);\n    document.addEventListener("visibilitychange", handleVisibility);\n\n    return () => {\n      window.clearInterval(interval);\n      window.removeEventListener("focus", refresh);\n      document.removeEventListener("visibilitychange", handleVisibility);\n    };\n  }, [canManageStudents]);\n\n`, '', 'AdminArea remove polling');
replaceOnce('scr/pages/AdminArea.tsx', '            <section className="admin-area-search-wrap admin-dashboard-search-wrap">', '            <section className={clsx("admin-area-search-wrap admin-dashboard-search-wrap", dashboardView === "students" && "is-student-sticky")}>', 'AdminArea sticky search');
replaceOnce('scr/pages/AdminArea.tsx', '                    {dashboardView === "library" ? <button type="button" className="admin-library-add" onClick={() => { resetExerciseDraft(); setExerciseDialogOpen(true); }} aria-label="Adicionar exercício">+</button> : null}', '                    {dashboardView === "library" ? (\n                      <button type="button" className="admin-library-add" onClick={() => { resetExerciseDraft(); setExerciseDialogOpen(true); }} aria-label="Adicionar exercício">+</button>\n                    ) : (\n                      <button type="button" className="admin-library-add admin-template-add-v159" onClick={() => navigate("/area-accqua/montar?modo=modelo")} aria-label="Criar novo modelo de treino" title="Criar novo modelo">+</button>\n                    )}', 'AdminArea model plus');

// 2) Reuse the canonical builder for a team model without binding a real student.
replaceOnce('scr/lib/admin.ts', '  avatarUrl: string;\n};\n\nexport type ExerciseLibraryItem', '  avatarUrl: string;\n};\n\nexport const WORKOUT_TEMPLATE_STUDENT_ID = "00000000-0000-0000-0000-000000000159";\n\nexport type ExerciseLibraryItem', 'template sentinel');
replaceOnce('scr/lib/admin.ts', 'export async function getWorkoutStudentById(\n  studentId: string,\n): Promise<WorkoutStudent | null> {\n  if (!isSupabaseConfigured) return null;\n', `export async function getWorkoutStudentById(\n  studentId: string,\n): Promise<WorkoutStudent | null> {\n  if (!isSupabaseConfigured) return null;\n\n  if (studentId === WORKOUT_TEMPLATE_STUDENT_ID) {\n    const { data: authData } = await supabase.auth.getUser();\n    const staffId = authData.user?.id ?? "";\n    return {\n      id: WORKOUT_TEMPLATE_STUDENT_ID,\n      fullName: "Modelo da equipe",\n      cpf: "", rg: "", registrationCode: "MODELO", gympassNumber: "",\n      membershipValidUntil: "", membershipPaymentDay: 0, membershipLastPayment: "",\n      membershipConfirmedAt: "", membershipNotes: "",\n      email: authData.user?.email ?? "", phone: "", emergencyPhone: "", birthDate: "",\n      objective: "Modelo reutilizável", role: "student", status: "active",\n      linkedProfessorId: staffId, linkedProfessorName: "Equipe ACCQUA", linkedProfessorEmail: authData.user?.email ?? "",\n      hasActiveWorkout: false, activeWorkoutCount: 0, workoutUpdatedAt: "", reviewAt: "",\n      programCode: "MODELO", avatarUrl: "",\n    };\n  }\n`, 'synthetic template context');
replaceOnce('scr/lib/admin.ts', 'export async function publishAdminProgram(\n  input: PublishAdminProgramInput,\n): Promise<void> {\n  if (!isSupabaseConfigured) {', `export async function publishAdminProgram(\n  input: PublishAdminProgramInput,\n): Promise<void> {\n  if (input.studentId === WORKOUT_TEMPLATE_STUDENT_ID) {\n    const modelName = input.programName.trim() || \`Modelo \${input.splitCode}\`;\n    await saveAdminProgramTemplate(input.staffId, {\n      name: modelName,\n      splitCode: input.splitCode,\n      payload: { programName: modelName, notes: input.notes, reviewAt: input.reviewAt, routines: input.routines, cardio: input.cardio },\n    });\n    return;\n  }\n\n  if (!isSupabaseConfigured) {`, 'save template through existing storage');

replaceOnce('scr/pages/WorkoutBuilderEntry.tsx', '  getWorkoutStudentById,\n  loadAdminProgramTemplates,', '  getWorkoutStudentById,\n  loadAdminProgramTemplates,\n  WORKOUT_TEMPLATE_STUDENT_ID,', 'entry sentinel import');
replaceOnce('scr/pages/WorkoutBuilderEntry.tsx', '  const studentId = searchParams.get("student") ?? "";\n  const returnStudentId = searchParams.get("returnStudent") ?? studentId;', '  const studentId = searchParams.get("student") ?? "";\n  const templateMode = searchParams.get("modo") === "modelo";\n  const returnStudentId = searchParams.get("returnStudent") ?? studentId;', 'entry template flag');
replaceOnce('scr/pages/WorkoutBuilderEntry.tsx', '  useEffect(() => {\n    if (!user?.id || !canManage || !studentId) return;\n    let alive = true;', '  useEffect(() => {\n    if (!user?.id || !canManage) return;\n    if (templateMode) { setBusy(false); return; }\n    if (!studentId) return;\n    let alive = true;', 'entry skip student load');
replaceOnce('scr/pages/WorkoutBuilderEntry.tsx', '  }, [canManage, studentId, user?.id]);', '  }, [canManage, studentId, templateMode, user?.id]);', 'entry deps');
replaceOnce('scr/pages/WorkoutBuilderEntry.tsx', '  if (!canManage || landingPath !== "/menu-teste") return <Navigate to={landingPath} replace />;\n  if (!student) return <Navigate to="/area-accqua" replace />;', '  if (!canManage || landingPath !== "/menu-teste") return <Navigate to={landingPath} replace />;\n  if (templateMode) return <Navigate to={`/area-accqua/montar/editor?student=${encodeURIComponent(WORKOUT_TEMPLATE_STUDENT_ID)}&modo=modelo`} replace />;\n  if (!student) return <Navigate to="/area-accqua" replace />;', 'entry same editor');

replaceOnce('scr/pages/AdminWorkoutBuilder.tsx', '  const studentId = searchParams.get("student") ?? "";\n  const returnStudentId =', '  const studentId = searchParams.get("student") ?? "";\n  const modelMode = searchParams.get("modo") === "modelo";\n  const returnStudentId =', 'builder model flag');
replaceOnce('scr/pages/AdminWorkoutBuilder.tsx', `  const returnToStudent = () => {\n    navigate(\n      returnStudentId\n        ? \`/area-accqua?student=\${returnStudentId}\`\n        : "/area-accqua",\n    );\n  };`, `  const returnToStudent = () => {\n    if (modelMode) { navigate("/area-accqua?section=templates"); return; }\n    navigate(\n      returnStudentId\n        ? \`/area-accqua?student=\${returnStudentId}\`\n        : "/area-accqua",\n    );\n  };`, 'builder model return');
replaceOnce('scr/pages/AdminWorkoutBuilder.tsx', '      setToast("Treino publicado para o aluno.");\n      window.setTimeout(returnToStudent, 900);', '      setToast(modelMode ? "Modelo salvo na biblioteca da equipe." : "Treino publicado para o aluno.");\n      window.setTimeout(returnToStudent, 900);', 'builder model feedback');

// 3) Final 1.5.9 visual layer.
write('scr/styles/build-1.5.9.css', `/* ACCQUA Sports — Build 1.5.9 */\n.admin-area-pending.has-pending,\n.admin-area-pending.has-pending:hover,\n.admin-area-pending.has-pending:focus-visible {\n  color: var(--text-on-active, #fff) !important;\n  border-color: var(--border-active, #f2c230) !important;\n  background: var(--surface-active, #1e3a5f) !important;\n}\n.admin-area-pending.has-pending small { color: inherit !important; }\n.admin-profile-view .admin-student-summary-list { gap: 12px !important; }\n.admin-profile-view .admin-student-summary-list .profile-menu-item { min-height: 44px; border-radius: 16px !important; }\n.admin-template-add-v159 { min-width: 40px; min-height: 40px; font-size: 22px !important; line-height: 1 !important; }\n@media (min-width:1024px) {\n  .admin-area-shell.is-dashboard .admin-dashboard-search-wrap.is-student-sticky {\n    position: sticky !important; top: 0 !important; z-index: 28 !important;\n    padding-top: 10px !important; padding-bottom: 10px !important;\n    background: var(--surface-deep, #04162f) !important;\n    backdrop-filter: none !important; -webkit-backdrop-filter: none !important;\n  }\n}\n@media (max-width:1023.98px) {\n  .admin-profile-view .admin-student-summary-list { grid-template-columns: repeat(3,minmax(0,1fr)) !important; gap: 12px !important; }\n  .admin-profile-view .admin-student-summary-list .profile-menu-item { height: 84px !important; min-height: 84px !important; border-radius: 16px !important; }\n}\n`);
replaceOnce('scr/main.tsx', 'import "./styles/build-1.5.8.css";', 'import "./styles/build-1.5.8.css";\nimport "./styles/build-1.5.9.css";', 'main css order');
replaceOnce('package.json', '"version": "1.5.8"', '"version": "1.5.9"', 'package version');
replaceOnce('package.json', 'verify-visual-contracts-1.5.8.mjs', 'verify-visual-contracts-1.5.9.mjs', 'package contracts');

// 1.5.9 contract file may already exist from staging; overwrite it with the final gate.
write('scripts/verify-visual-contracts-1.5.9.mjs', `import "./verify-visual-contracts-1.5.8.mjs";\nimport fs from "node:fs";\nimport path from "node:path";\nimport { fileURLToPath } from "node:url";\nconst root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");\nconst failures=[]; const passes=[]; const read=(file)=>fs.readFileSync(path.join(root,file),"utf8");\nconst requireMatch=(id,file,pattern,note)=>pattern.test(read(file))?passes.push(id):failures.push(\`\${id} — \${note} (\${file})\`);\nconst requireAll=(id,file,patterns,note)=>patterns.every((pattern)=>pattern.test(read(file)))?passes.push(id):failures.push(\`\${id} — \${note} (\${file})\`);\nconst requireAbsent=(id,file,pattern,note)=>!pattern.test(read(file))?passes.push(id):failures.push(\`\${id} — \${note} (\${file})\`);\nconst css="scr/styles/build-1.5.9.css", area="scr/pages/AdminArea.tsx", entry="scr/pages/WorkoutBuilderEntry.tsx", builder="scr/pages/AdminWorkoutBuilder.tsx", admin="scr/lib/admin.ts";\nrequireMatch("159/version","package.json",/"version":\\s*"1\\.5\\.9"/,"package não está em 1.5.9");\nrequireMatch("159/contracts","package.json",/verify-visual-contracts-1\\.5\\.9\\.mjs/,"npm não executa contratos 1.5.9");\nrequireMatch("159/css-last","scr/main.tsx",/build-1\\.5\\.8\\.css";\\s*\\nimport "\\.\\/styles\\/build-1\\.5\\.9\\.css";/,"camada 1.5.9 não é a última");\nrequireAll("159/roster-stable",area,[/studentsHydratedRef/,/if \\(!studentsHydratedRef\\.current\\) setStudentsLoading\\(true\\)/],"lista não preserva dados");\nrequireAbsent("159/no-roster-poll",area,/setInterval\\(refresh,\\s*15000\\)|addEventListener\\("focus",\\s*refresh\\)|visibilitychange/,"polling antigo ainda existe");\nrequireAll("159/sticky-search",area,[/is-student-sticky/,/admin-dashboard-search-wrap/],"busca/filtros não são sticky");\nrequireAll("159/sticky-opaque",css,[/is-student-sticky[\\s\\S]*?position:\\s*sticky/,/top:\\s*0/,/background:\\s*var\\(--surface-deep/],"sticky não é opaco");\nrequireAll("159/pending-active",css,[/admin-area-pending\\.has-pending/,/--surface-active/,/--text-on-active/,/--border-active/],"pendentes sem tokens ativos");\nrequireAll("159/profile-grid",css,[/admin-student-summary-list[\\s\\S]*?gap:\\s*12px/,/profile-menu-item[\\s\\S]*?border-radius:\\s*16px/,/min-height:\\s*44px/,/height:\\s*84px/],"grid do perfil regrediu");\nrequireAll("159/template-plus",area,[/admin-template-add-v159/,/\\/area-accqua\\/montar\\?modo=modelo/,/Criar novo modelo de treino/],"Modelos sem botão +");\nrequireAll("159/template-entry",entry,[/templateMode/,/WORKOUT_TEMPLATE_STUDENT_ID/,/montar\\/editor\\?student=/,/modo=modelo/],"modo modelo não converge no editor");\nrequireAll("159/template-context",admin,[/WORKOUT_TEMPLATE_STUDENT_ID/,/fullName:\\s*"Modelo da equipe"/,/objective:\\s*"Modelo reutilizável"/],"contexto de modelo ausente");\nrequireAll("159/template-save",admin,[/input\\.studentId === WORKOUT_TEMPLATE_STUDENT_ID/,/saveAdminProgramTemplate\\(input\\.staffId/,/programName:\\s*modelName/],"modelo não usa storage existente");\nrequireAll("159/template-return",builder,[/modelMode/,/section=templates/,/Modelo salvo na biblioteca da equipe/],"editor não retorna para Modelos");\nif(failures.length){console.error("\\nACCQUA Build 1.5.9 — contratos FALHARAM:\\n"); failures.forEach((f)=>console.error(\` - \${f}\`)); process.exit(1);}\nconsole.log(\`ACCQUA Build 1.5.9 — \${passes.length} contratos validados.\`);\n`);
console.log('Clean Build 1.5.9 functional patch applied.');
